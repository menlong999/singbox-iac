# CLAUDE.md

Start here:

1. `/Users/lvyuanfang/Code/SingBoxConfig/docs/agent-context.md`
2. `/Users/lvyuanfang/Code/SingBoxConfig/README.md`
3. `/Users/lvyuanfang/Code/SingBoxConfig/openspec/project.md`

Use progressive disclosure:

- do not load the whole repo at once
- read the task-specific docs listed in `docs/agent-context.md`
- follow the active OpenSpec change before editing code

Current product phase:

- stabilize the existing `system-proxy + process-proxy + real-ip` path
- improve runtime recovery and diagnostics
- improve natural-language authoring with patch semantics and bundle intelligence
- defer `tun + fake-ip` as the next phase, not the current default
