# Tasks

- [ ] define a stable on-disk representation for layered authored intent
- [ ] change `use` default behavior from replacement to patch semantics
- [ ] add `--replace` for explicit full replacement
- [ ] add tests covering additive update vs replacement behavior
- [ ] update README and natural-language authoring docs with the new semantics
