# Tasks

- [ ] add a new diagnostics command and module
- [ ] collect runtime, proxy, listener, schedule, and transaction evidence
- [ ] add a small set of best-effort DNS and local-network checks
- [ ] add CLI tests for healthy and unhealthy summaries
- [ ] document when to use `doctor` vs `status` vs the new diagnostics command
