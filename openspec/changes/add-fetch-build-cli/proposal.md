# Change Proposal: add-fetch-build-cli

## Summary

Implement remote subscription fetching, local config loading, and CLI commands for building generated configs.

## Scope

- fetch subscriptions from HTTP URLs
- load user YAML config
- implement `init`, `build`, and `update` command behavior
- write generated config artifacts to the configured output path

