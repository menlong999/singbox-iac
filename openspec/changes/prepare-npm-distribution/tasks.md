# Tasks

- [x] add install-ready package metadata and CLI shebang
- [x] document local tarball installation and publish prerequisites
- [x] add tests that guard package metadata and CLI entrypoint expectations
- [x] run `npm test`, `npm run build`, `npm pack`, and a clean-directory install smoke
- [x] record the validation results
