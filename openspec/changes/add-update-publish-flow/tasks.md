## Tasks

- [x] Add an update module that composes build, verification, and apply.
- [x] Implement the `update` CLI command on top of the module.
- [x] Add automated tests for the update flow.
- [x] Run static checks, unit tests, and a real-subscription update flow against temporary publish targets.
