# Tasks

- [ ] add runtime watchdog configuration and narrow default interval
- [ ] implement proxy reassert behavior for healthy `system-proxy` runtime
- [ ] surface watchdog health in `status`
- [ ] add tests for proxy drift followed by successful reassert
- [ ] document watchdog behavior and limits
