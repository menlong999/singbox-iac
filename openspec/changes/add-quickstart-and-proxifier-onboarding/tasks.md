# Tasks

- [x] add a reusable setup flow entrypoint that can be called by both `setup` and `quickstart`
- [x] add a `quickstart` CLI command that maps a subscription URL and one routing sentence into the ready flow
- [x] add Proxifier helper generation utilities and a `proxifier scaffold` command
- [x] add `proxifier bundles` to list built-in process presets
- [x] generate Proxifier helper files automatically from onboarding prompts that imply process-aware routing
- [x] cover the new behavior with CLI tests
- [x] update README, runtime docs, and CLI specs
- [x] run repository validation and one real quickstart smoke
- [x] publish a new npm patch version after validation passes
