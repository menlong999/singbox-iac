# Tasks

- [ ] decide the minimum viable macOS signal source for wake and network-change recovery
- [ ] implement recovery ladder for `system-proxy` runtime
- [ ] add tests for recovery decision logic
- [ ] update runtime docs with expected wake/network recovery behavior
