## Tasks

- [x] Update builder config schema to support explicit selector defaults and configured verification scenarios.
- [x] Update the compiler so `Process-Proxy`, `Stitch-Out`, `AI-Out`, and `Dev-Common-Out` can follow user-defined defaults.
- [x] Update local and example builder configs to encode the current user journeys.
- [x] Update the verification harness to consume configured scenarios from builder config.
- [x] Expand tests to cover explicit selector defaults and scenario-driven verification.
- [x] Run `typecheck`, `lint`, unit tests, rebuild with the real subscription, and run the full `verify` closed loop.
