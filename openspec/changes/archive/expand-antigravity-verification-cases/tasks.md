## Tasks

- [x] Add stable Antigravity auth, product, and Google API scenarios to local builder config.
- [x] Mirror the expanded scenario set in the example builder config and CLI fallback config.
- [x] Update Antigravity endpoint documentation with observed runtime domains and journey buckets.
- [x] Run `typecheck`, `lint`, `test`, and a full real-subscription `verify`.
- [x] Record validation results and mark tasks complete.
