# Tasks

- [x] add `ProxifierBundleSpec` model types
- [x] convert built-in process presets into declarative bundle specs
- [x] extend `proxifier bundles` with `show` and `render`
- [x] update `proxifier scaffold` to resolve bundle specs internally
- [x] add built-in bundle specs for Antigravity, Cursor, VS Code, Claude Code, Gemini CLI, Codex, and Copilot CLI
- [x] add tests for bundle listing, inspection, and rendering
- [x] update proxifier onboarding docs and README examples
- [x] run CLI smoke for bundle render output
