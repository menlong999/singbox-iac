# Tasks

- [x] define a stable on-disk representation for layered authored intent
- [x] change `use` default behavior from replacement to patch semantics
- [x] add `--replace` for explicit full replacement
- [x] add tests covering additive update vs replacement behavior
- [x] update README and natural-language authoring docs with the new semantics
