# Tasks: use-conflict-averse-default-ports

- [x] select a high port pair that does not collide with the current local listeners
- [x] update local config, example config, and CLI fallback defaults
- [x] update automated tests to assert the new defaults
- [x] validate `build -> check -> run` with the new ports
