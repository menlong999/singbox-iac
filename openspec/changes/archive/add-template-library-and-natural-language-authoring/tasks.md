## Tasks

- [x] Add built-in rule templates for common developer and video-site scenarios.
- [x] Add a natural-language generator that emits DSL rules, inferred templates, notes, and optional schedule intervals.
- [x] Add `templates` and `author` CLI commands.
- [x] Allow `author` to update the builder config and optionally install a launchd schedule.
- [x] Make repeated authoring safe by allowing schedule replacement with `--force-schedule`.
- [x] Add unit and CLI tests for template selection and authoring flows.
- [x] Update README and authoring docs for templates and natural-language usage.
- [x] Run `typecheck`, `lint`, `test`, and a real smoke flow covering `author -> verify -> update`.
- [x] Record validation results and mark tasks complete.
