# Tasks

- [x] add ambiguity detection rules to natural-language authoring
- [x] add `--strict` to `author` and `use`
- [x] add `--emit-intent-ir` output to `author` and `use`
- [x] add `--diff` output based on `IntentIR`, config defaults, and generated rules
- [x] add tests for ambiguous prompt rejection
- [x] add tests for `IntentIR` emission and diff rendering
- [x] update README and natural-language authoring docs
- [x] run repository validation and one real authoring smoke with `--strict` and `--diff`
