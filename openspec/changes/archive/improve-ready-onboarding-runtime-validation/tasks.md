# Tasks

- [x] extend `setup` with a foreground run mode for successful onboarding flows
- [x] add a visible Chrome launch helper for onboarding validation
- [x] add prompt-driven verification-target selection for onboarding
- [x] switch automated runtime verification from browser-driven noise to request-driven checks
- [x] validate the full `setup --ready --run` flow through a published npm install with a real subscription and a real routing prompt
