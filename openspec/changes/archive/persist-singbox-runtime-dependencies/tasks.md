# Tasks

- [x] Extend builder config schema with persisted runtime dependency fields.
- [x] Add a runtime dependency persistence helper for setup/doctor flows.
- [x] Make runtime commands prefer persisted paths before ambient discovery.
- [x] Export persisted dependency paths into the update LaunchAgent environment.
- [x] Improve missing-`sing-box` diagnostics with concrete macOS install guidance.
- [x] Add tests covering persisted-path precedence and invalid persisted-path fallback.

