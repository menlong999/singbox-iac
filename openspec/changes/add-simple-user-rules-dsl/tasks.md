## Tasks

- [x] Add a YAML-based user-rules loader with schema validation.
- [x] Insert custom rules into compiler routing phases and validate rule targets.
- [x] Update examples, local config, and DSL documentation to reflect the final syntax.
- [x] Add tests covering rule parsing and route insertion.
- [x] Create a real local custom-rules file and include a runtime verification scenario proving the DSL is active.
- [x] Run `typecheck`, `lint`, `test`, and a real-subscription `verify`.
- [x] Record validation results and mark tasks complete.
