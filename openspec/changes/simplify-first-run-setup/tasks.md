# Tasks

- [x] add a one-step `setup` command
- [x] make default config discovery include the user-level config path
- [x] fix `init` so it can find packaged examples after global install
- [x] add tests for global-install-style `init` and `setup`
- [x] document the new first-run path
- [x] run real command-level validation and record the result
